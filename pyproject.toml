[project]
name = "munich-transit"
version = "0.1.0"
description = "Munich public transit reliability: MVV GTFS schedules + MVG live departures"
requires-python = ">=3.11"
dependencies = [
    "duckdb>=1.1",
    "pandas>=2.2",
    "requests>=2.31",
]
